import { ShowLastDigitsPipe } from './show-last-digits.pipe';

describe('ShowLastDigitsPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowLastDigitsPipe();
    expect(pipe).toBeTruthy();
  });
});
